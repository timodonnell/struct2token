# Data-only config (for overriding defaults)

data:
  mmcif_dir: "~/tim1/helico-data/raw/mmCIF"
  index_path: "data/index.parquet"
  cache_dir: "data/cache"
  max_atoms: 8000
  min_atoms: 10
  val_fraction: 0.05
  num_workers: 4
