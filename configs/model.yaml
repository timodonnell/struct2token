# Model-only config (for overriding defaults)

model:
  encoder:
    d_model: 256
    n_layers: 4
    n_heads: 8
  fsq:
    levels: [8, 5, 5, 5]
  decoder:
    d_model: 512
    n_layers: 12
    n_heads: 8
  max_seq_len: 8192
  n_tokens: 128
  cfg_drop_rate: 0.05
